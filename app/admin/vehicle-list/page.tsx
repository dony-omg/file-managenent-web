'use client';

import TableVehicleList from './tableVehicle';

const VehicleListPage = () => {
  // fetch('/api/vehicles', {
  //   method: 'GET',
  //   headers: {
  //     'Content-Type': 'application/json'
  //   }
  // }).then((data: any) => {
  //   console.log(data);
  // });

  return <TableVehicleList data={''} />;
};

export default VehicleListPage;
